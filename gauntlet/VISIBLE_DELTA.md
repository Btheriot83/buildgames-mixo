# Visible delta — Hot Metal Press (Quoin Lock)

**Rule:** A pass fails if the previous live demo cannot be distinguished in ~3 seconds.  
**Identity:** Still **Quoin Lock** (no reseed). Amplify only.

## Screenshots
| | Path |
| --- | --- |
| **Before** (prior live) | `gauntlet/screenshots/before-visible-delta.png` |
| **After** (billboard amplify) | `gauntlet/screenshots/after-visible-delta.png` |

## Five visible deltas (spot in &lt;3s)
1. **Layout** — Split **billboard stage**: copy + create on the left, materials column sticky on the right (was single narrow column with imagery buried below).
2. **Type scale** — Fraunces H1 jumps to **billboard size** (`clamp ~3.6–7.5rem`, tight leading, line break) — previous was soft ~2.6–4.6rem marketing size.
3. **Materials** — Full-width **acid chartreuse make-ready tape** bar at top (`MAKE-READY / QUOIN LOCK / HOT METAL / PROOF DESK`) + oversized registration marks — absent before.
4. **Stage chrome** — Create panel gets **wine offset shadow**, thicker ink border, acid tape tab on the panel; brand mark scales to **72px** with wine block shadow.
5. **Imagery** — Proof-sheet Imagine plate is **above-the-fold hero** with acid caption bar + ink-loop matte beside it — previously a small strip under the fold.

## Not counted (docs-only)
IDENTITY.md / copy tables / +3% CSS without stage change — excluded per PIPELINE visibility gate.
