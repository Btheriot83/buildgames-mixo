# Define — Hot Metal Press

## Identity
See `IDENTITY.md` — **Quoin Lock** (locked). Critic below grades that aesthetic against a studio bar, and execution against live Mixo.

## Technique 4 — Image assets (real)
| Asset | Path | Source |
| --- | --- | --- |
| Proof sheet hero | `public/textures/imagine/proof-sheet.png` | Higgsfield gpt_image_2_5 |
| Empty type still | `public/textures/imagine/empty-type.png` | Higgsfield gpt_image_2_5 |
| Quoin / chase mark | `public/textures/imagine/quoin-lock.png` | Higgsfield gpt_image_2_5 |

CSS blobs alone rejected.

## Technique 5 — Video / motion
- `ink-loop.mp4` matted as “Make-ready · ink film” in desktop press rail (hidden &lt;960px so the brief stays first)
- Press meter + ram during generate; stamp overlay on ZIP/HTML export
- transitions.dev: HeroReveal stagger, ThinkingLine, SpinningCounter where still present in editor — not a substitute for imagery

## Mobbin
Mobbin MCP returned paid-plan / rate-limit on 2026-09-14 PT. Primary bar = live original screenshots (`gauntlet/screenshots/mixo/r0-bar-mixo.png`). Category comps deferred; do not invent Mobbin citations.

## Technique 3 — Fresh-context screenshot critic (Phase A)

**Inputs only:** unlabeled screenshots — Mixo.io bar + candidate Phase A home (`mixo-phaseA-home.png` / `gauntlet/screenshots/mixo/`).

### Aesthetic naming
Candidate reads as letterpress shop desk / proof press — not generic SaaS.

### Studio bar (imagined)
Pentagram-meets-print-studio: one unmistakable prompt, stamped result visible within a gesture, materials photographic, type disciplined (display only in display sizes), zero decorative collision with the input.

### Biggest gaps (Phase A critic)
1. **Job clarity vs Mixo** — Mixo’s single prompt dominates the viewport; candidate still fights itself when press imagery stacks above the brief on mid widths (layout debt).
2. **Result immediacy** — Mixo shows a generated site as product hero; we still route to an editor after stamp (acceptable for personal tool, loses blind “wow”).
3. **Density** — letterpress chrome (reg marks, dual media) can outshout the textarea.
4. **AI tells** — Fraunces no longer on body (good); watch for FAQ filler in LLM output; no purple/glow detected.

### Score vs studio bar for Quoin Lock
**6.4 / 10** — distinctive materials and Imagine assets land; core Mixo job not yet winning blind A/B. Baseline honesty vs Brandon: start ~3.8, Phase A earns to ~6.4 on craft identity, not contest win.

`criticFreshContext: true` — graded from pixels/docs only, not builder self-score.
